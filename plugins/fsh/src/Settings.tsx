import { ScrollView } from "react-native";

import { Forms } from "@vendetta/ui/components";
const { FormText } = Forms;

export default () => (
    <ScrollView>
        <FormText>
            FSH FSH
        </FormText>
        <FormText>
            Idk what to add here or do storage so :p
        </FormText>
    </ScrollView>
)
